<project name="Grunt Salesforce Ant Deploy" default="deploy" xmlns:sf="antlib:com.salesforce">

  <target name="deploy" depends="proxy">
    <sf:deploy 
      username="<%= user %>" 
      password="<%= pass %>" 
      serverurl="<%= serverurl %>" 
      maxPoll="<%= maxPoll %>" 
      pollWaitMillis="<%= pollWaitMillis %>" 
      deployroot="<%= root %>" 
      checkonly="<%= checkOnly %>" 
      runAllTests="<%= runAllTests %>"
      rollbackOnError="<%= rollbackOnError %>">
      <% _.forEach(tests, function(t) { %><runTest><%= t %></runTest><% }); %>
    </sf:deploy>
  </target>

<target name="proxy">
      <property name="proxy.port" value="8080"/>
      <property name="proxy.user" value="Sundararajansr"/>
      <property name="proxy.pass" value="December2015*"/>
      <setproxy proxyhost="bcproxy.us.dnb.com" proxyport="${proxy.port}"
        proxyuser="${proxy.user}" proxypassword="${proxy.pass}"/>
  </target>

</project>